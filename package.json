{
  "name": "edu-stack-sandbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/summary.js && concurrently -n \"INFO,BE,FE,MCP,MAIL\" -c \"cyan,green,blue,yellow,magenta\" \"node scripts/summary.js --delay=10000\" \"npm run start:dev -w backend\" \"npm run dev -w frontend\" \"npm run dev -w mcp-server\" \"npx maildev --smtp 1025 --web 1081\"",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "db:sync": "node scripts/sync-schema-provider.js",
    "db:generate": "npm run db:sync && npm run prisma:generate -w backend && npm run prisma:generate -w mcp-server",
    "db:init": "npm run db:sync && cd apps/backend && rm -rf .wrangler/state/v3/d1 && npx prisma migrate diff --from-empty --to-schema ./prisma/schema.prisma --script > ./prisma/schema.sql && npx wrangler d1 execute edustack_db --local --file=./prisma/schema.sql && cd ../.. && npm run db:generate",
    "db:deploy": "npm run db:sync && cd apps/backend && npx prisma migrate diff --from-empty --to-schema ./prisma/schema.prisma --script > ./prisma/schema.sql && npx wrangler d1 execute edustack_db --remote --file=./prisma/schema.sql",
    "db:studio": "npm run db:sync && cd apps/backend && npx prisma studio",
    "seed:demo": "npm run seed:demo -w backend",
    "test:e2e": "npm run test:e2e -w frontend",
    "test:e2e:ui": "npm run test:e2e:ui -w frontend",
    "test:e2e:install": "npm run test:e2e:install -w frontend"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
